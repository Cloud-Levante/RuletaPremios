# Puerto en el que se expone la app (por defecto 8080)
APP_PORT=8080

# Admin panel credentials
# Si no se configuran, el primer acceso al engranaje mostrara un formulario de registro
# Para generar el hash: echo -n "tu_password" | shasum -a 256
VITE_ADMIN_USER=admin
VITE_ADMIN_PASS_HASH=
